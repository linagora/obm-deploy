---
# This file stores default values for role variables
# Please don't edit it
# Values can be updated in config.yml, groups_vars/opushservers
# or in host_vars/fqdn_of_your_host or each of them

ldap_host: "{{ groups['ldapservers'][0] | mandatory }}"
cassandra_host: "{% if major_version | version_compare('3.0', '>=') %}{{ groups['cassandraservers'][0] | mandatory }}{% endif %}"
