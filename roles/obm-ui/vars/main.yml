---
# This file stores default values for role variables
# Please don't edit it
# Values can be updated in config.yml, groups_vars/uiservers
# or in host_vars/fqdn_of_your_host or each of them

entry_point: "{{ obm_external_url | mandatory }}"
ldap_host: "{{ groups['ldapservers'][0] | mandatory }}"
obm_sync_host: "{{ groups['syncservers'][0] | mandatory }}"
opush_host: "{{ groups['opushservers'][0] | mandatory }}"
